;document.addEventListener('DOMContentLoaded',function(undefined) {

var
	form = document.forms[0],
	input = document.forms[0].getElementsByTagName("input")[0],
$$;

function progress(e) {

}

input.addEventListener("change", function(e) {
	var
		x = new XMLHttpRequest(),
	$$;
	// Add content-type of the file.type.
	// Add all fields from form.
	// Upload xhr, attach progress listener.
});


});